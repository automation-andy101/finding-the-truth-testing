[behave]
lang=en

# Define the paths where feature files are located
paths=features/ui_landing_page.feature
    features/ui_case_selection.feature

# Allure Reporting configuration
format=allure_behave.formatter:AllureFormatter
outfiles=allure-results
